<p id="notice"><%= notice %></p>

<h1>Form Excise Taxes</h1>

<table>
  <thead>
    <tr>
      <th>Formreferencenumber</th>
      <th>Formeffectivedate</th>
      <th>Cusname</th>
      <th>Signflag</th>
      <th>Formdata</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @form_excise_taxes.each do |form_excise_tax| %>
      <tr>
        <td><%= form_excise_tax.formreferencenumber %></td>
        <td><%= form_excise_tax.formeffectivedate %></td>
        <td><%= form_excise_tax.cusname %></td>
        <td><%= form_excise_tax.signflag %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Form Excise Tax' %>
